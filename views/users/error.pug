extends layout

block content
    div(class="container")
        h1= error.status || 500
        div(class="row")
            div(class="col-lg")
                h2= error.message
                pre #{error.stack}
