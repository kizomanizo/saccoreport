extends layout

block content
    div(class="container")
        h2 #{title}
        div(class="row")
            div(class="col-lg")
                if error
                    p(class="error") #{error}
                form(action="/users/add" enctype="multipart/form-data" method="post")
                    div(class="form-group")
                        label(for="csv") Upload a file
                        input(type="file" class="form-control-file" name="csv")
                        small(id="emailHelp" class="form-text text-muted") Use the form above to upload a CSV file
                    input(type="submit" value="Upload CSV File" class="btn btn-default")